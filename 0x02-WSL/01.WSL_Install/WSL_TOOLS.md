
# 更新软件源
```bash
sudo apt update
```

# 安装 LibreOffice 完整套件
```bash
sudo apt install libreoffice
```

## 仅安装 Writer（文字处理）和 Calc（表格）PPT 和 pdf
```bash
sudo apt install libreoffice-writer libreoffice-calc libreoffice-impress libreoffice-pdfimport
```

## 查看版本
```bash
libreoffice --version
```
